# ============================================
# Interview Bot - Production Environment
# ============================================
# Copy this file to .env.production and fill in the values:
#   cp .env.production.example .env.production
#   chmod 600 .env.production
#
# Run with:
#   docker compose -f docker-compose.yml -f docker-compose.production.yml --env-file .env.production up -d --build

# ── Database ──
POSTGRES_PASSWORD=           # openssl rand -base64 32

# ── Auth ──
APP_PASSWORD=                # Strong password for initial user
JWT_SECRET=                  # openssl rand -base64 32

# ── Domain ──
DOMAIN=interviewbot.kr       # Your domain (used in NEXT_PUBLIC_APP_URL)

# ── Stripe ──
STRIPE_SECRET_KEY=sk_live_...
STRIPE_PUBLISHABLE_KEY=pk_live_...
STRIPE_WEBHOOK_SECRET=whsec_...
STRIPE_PRICE_PRO_MONTHLY=price_...
STRIPE_PRICE_PRO_YEARLY=price_...

# ── Cron ──
CRON_SECRET=                 # openssl rand -base64 32

# ── Email (Resend) ──
RESEND_API_KEY=re_...
EMAIL_FROM=InterviewBot <noreply@interviewbot.kr>

# ── Security ──
ADMIN_ALLOWED_IPS=127.0.0.1,::1    # Comma-separated allowed IPs for /admin
TRUSTED_PROXY_IPS=172.17.0.1       # Docker bridge network IP

# ── AI ──
AI_MODEL=claude-haiku-4-5-20251001
AI_PRIMARY_PROVIDER=api             # proxy | api (api requires ANTHROPIC_API_KEY)
ANTHROPIC_API_KEY=sk-ant-...        # Required when AI_PRIMARY_PROVIDER=api
AI_PROXY_URL=http://localhost:3456  # Fallback proxy (optional)

# ── Monitoring Scripts ──
# Used by scripts/check-*.sh (set in crontab environment)
# RESEND_API_KEY=re_...             # Same as above
# MONITOR_DOMAIN=interviewbot.kr
# MONITOR_EMAIL=admin@interviewbot.kr
